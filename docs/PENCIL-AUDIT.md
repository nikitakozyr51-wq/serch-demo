# Сверка с Pencil: что нашли и что осталось

Снято 07.08.2026 одиннадцатью агентами: пять шли по охвату кадров,
шесть мерили геометрию отрисованных экранов против кадров файла.

Файл существует ровно затем, чтобы следующий разговор не повторял эту
работу с нуля. Каждый пункт — либо уже закрытая правка, либо конкретное
дело со снятым числом.

## Охват: 235 экранов файла

| | |
|---|---|
| собрано и сослано на кадр | 149 |
| собрано, но ссылки нет | 7 |
| собрано наполовину | 20 |
| не собрано | 56 |
| отложено с причиной | 4 письма |

`bun run pencil:coverage` считает только первую строку: он ищет упоминание
идентификатора кадра в коде. На вопрос «совпадает ли экран с кадром»
отвечает сверка геометрии, на «достижимо ли состояние в продукте» —
только глаз: ни одна из проверок этого не видит.

## Собраны, но ссылки на кадр в коде нет

Проставить `Снято с id` в шапке файла — и число охвата станет правдой.

- `MULT9` СОСТОЯНИЕ · Командная палитра ⌘K — D:/серч/webapp/src/features/cabinet/CommandPalette.tsx (кадр подписан в шапке файла как rs1pv)
- `M9BgZ1` СОСТОЯНИЕ · Карта горячих клавиш — D:/серч/webapp/src/features/cabinet/dialogs.tsx:42 (HotkeysDialog, подписан как L0qKK — внутренность кадра M9BgZ1)
- `EKtRe` МОБАЙЛ · Загрузка списка — D:/серч/webapp/src/mobile-today-screens.tsx:612 (MobileListLoadingPage), маршрут /m/list-loading — routes.tsx:549, продуктовый, живёт и в собранной версии
- `LML7R` ПРОФИЛЬ · Безопасность и сеансы — D:/серч/webapp/src/profile-security-screen.tsx, экспорт LoginPolicyPage, адрес /profile/login-policy — вкладка «Безопасность» раздела «Профиль», выход «Завершить все сеансы» заменён кнопкой «Выйти из аккаунта» в шапке
- `E5kwP` КАБИНЕТ · Выдача, узкий экран 1280 — D:/серч/webapp/src/features/listings/useFilterCollapse.ts: ниже 1360 колонка фильтров сворачивается в полоску над выдачей, значит на 1280 состояние достижимо само
- `tkltB` ПОИСК · Сохранённые поиски — первый вход — D:/серч/webapp/src/searches-screen.tsx, функция FirstSearch (строка 303), подставляется в SearchesPage при savedSearches.length === 0
- `WrBIO` СОСТОЯНИЕ · Подборок ещё нет — D:/серч/webapp/src/collections-screens.tsx:214, адрес /collections

## Собраны наполовину

Чаще всего окно СОБРАНО, но его нечем открыть: лежит карточкой на стенде
`/dialogs`, а в продукте нет ни выделения строк, ни кнопки, которая его
вызывает. Это не дизайн, это недостающая связь.

### `GKHtA` СОСТОЯНИЕ · Баланс 0

Поведение есть, вида нет. Списание при нуле действительно останавливается: D:/серч/webapp/src/features/auth/demo-session.ts:341 возвращает «no-money», карточка объекта уводит на пополнение (D:/серч/webapp/src/object-card-screen.tsx:108-115), в шапке стоит «Баланс агентства 0 ₽» (D:/серч/webapp/src/features/cabinet/CabinetHeader.tsx:104). НЕ ХВАТАЕТ самой плашки над выдачей: заголовка «Баланс агентства 0 ₽. Раскрытие контактов остановлено», подписи «Выдача, фильтры, уже раскрытые контакты и звонки продолжают работать» и кнопки «Сообщить руководителю». Сейчас на выдаче нажатие при нуле пишет только невидимый атрибут data-last-action (D:/серч/webapp/src/search-screen.tsx:501) — человек не видит ничего.

### `JpcQn` СОСТОЯНИЕ · Контакт отозван собственником

Есть строка выдачи с отозванным контактом: D:/серч/webapp/src/data/search-rows.ts:225 задаёт действие {kind:"blocked", label:"Контакт отозван", quiet:true}, плашку рисует D:/серч/webapp/src/features/listings/ListingRow.tsx:101. НЕ ХВАТАЕТ нарисованной колонки действия в карточке объекта: карточка /object всегда показывает «Раскрыть контакт · 199 ₽» (D:/серч/webapp/src/object-card-screen.tsx:160) и про отзыв не знает вовсе. Нет плашки «Собственник запросил исключение номера 22.07.2026» с объяснением автоматического возврата 199 ₽, нет выключенной кнопки с замком «Контакт отозван» и нет абзаца про маскировку номера до +7 (9••) •••-••-67 в истории звонков.

### `vEqRl` СОСТОЯНИЕ · Стоп-лист, просил не звонить

Сам стоп-лист живой: отметка ставится в панели звонка (D:/серч/webapp/src/call-mode-screen.tsx:246, 720-739), строка выдачи после неё блокируется подписью «Просил не звонить» (D:/серч/webapp/src/search-screen.tsx:160-167), реестр открывается на /agency/refusals (D:/серч/webapp/src/agency-refusals-screen.tsx). НЕ ХВАТАЕТ колонки действия в карточке объекта: /object для адреса из стоп-листа всё равно предлагает «Раскрыть контакт · 199 ₽» — object-card-screen.tsx не проверяет стоп-лист. Нет плашки «Собственник просил не звонить · 12.07» с именем отметившего, нет кнопки «Раскрытие недоступно» и нет правила про то, что снять отметку не может никто.

### `pJX9B` СОСТОЯНИЕ · Диалог массового раскрытия

Сам диалог собран: D:/серч/webapp/src/dialog-screens.tsx:127 (DisclosureBulkDialog, подписан как q5xX5 — это как раз внутренность кадра pJX9B), открывается по адресу /dialogs. НЕ ХВАТАЕТ достижимости в продукте: диалог там не всплывает, потому что массового раскрытия нет — на выдаче (D:/серч/webapp/src/search-screen.tsx) нет ни выбора строк, ни чекбоксов, ни кнопки «Раскрыть N контактов». На /dialogs он лежит карточкой в колонке стенда, а не окном поверх выдачи.

### `aT2KC` СОСТОЯНИЕ · Панель массовых действий

Содержимое собрано: D:/серч/webapp/src/dialog-screens.tsx:145 (SelectionDialog «Выбрано 12 объектов», подписан как z9waH — внутренность кадра aT2KC), видно по адресу /dialogs. НЕ ХВАТАЕТ двух вещей. Первая: выбора объектов в выдаче нет вовсе — в search-screen.tsx ни выделения, ни счётчика выбранного, значит панель нечем вызвать. Вторая: даже собранное — это карточка диалога 520 px, а не нижняя панель поверх выдачи, которая сдвигает список вверх на свою высоту, как описано в самом кадре.

### `hS4nq` СОСТОЯНИЕ · Экспорт выбранного

Диалог собран и работает: D:/серч/webapp/src/dialog-screens.tsx:337 (ExportDialog, подписан как t0M9V9 — внутренность кадра hS4nq), выбор XLSX/CSV переключается по-настоящему, открывается по адресу /dialogs. НЕ ХВАТАЕТ вызова: в выдаче нет ни выбора двенадцати объектов, ни кнопки «Экспорт» — поиск по слову «Экспорт» вне dialog-screens.tsx находит только «Экспорт для проверки» в стоп-листе (agency-refusals-screen.tsx:114), это другое действие.

### `PcK62` СОСТОЯНИЕ · Сохранить поиск

Диалог собран целиком и живой: D:/серч/webapp/src/dialog-screens.tsx:238 (SaveSearchDialog, подписан как q7Yehb — внутренность кадра PcK62), чипы «Личный/Агентства» и «Сразу/Раз в день утром/Выключено» переключаются, открывается по адресу /dialogs. НЕ ХВАТАЕТ вызова из продукта: кнопка «Сохранить поиск» в колонке фильтров (D:/серч/webapp/src/features/listings/FilterPanel.tsx:337) сохраняет молча и сразу уводит на /searches (D:/серч/webapp/src/search-screen.tsx:672-676) — ни имени, ни автора, ни расписания уведомлений человека не спрашивают. Телефонный двойник при этом всплывает по-настоящему: /m/save-search, но это другой кадр (sN5wC).

### `HkmNL` СОСТОЯНИЕ · Глобальный поиск из шапки

Само поле в шапке стоит на месте и нажимается: D:/серч/webapp/src/features/cabinet/CabinetHeader.tsx:80-97, подпись «Адрес, телефон или номер» и ⌘K справа. НЕ ХВАТАЕТ нарисованной панели результатов: нажатие открывает командную палитру (CabinetShell.tsx:78 → requestPalette), а у неё группы «ОБЪЕКТЫ · ДЕЙСТВИЯ · ПЕРЕЙТИ», а не «ОБЪЕКТЫ · ПО ТЕЛЕФОНУ». Нет группы найденного по телефону со состоянием «0 ₽ повторно» и нет пояснения «Телефон находится только среди уже раскрытых агентством контактов: нераскрытые по номеру не ищутся» — то есть граница продукта на компьютере нигде не названа. На телефоне это собрано: /m/global-search, D:/серч/webapp/src/mobile-search-extra-screens.tsx:824-885, но это другой кадр (bx94j).

### `nPbMM` СОСТОЯНИЕ · Заявка на возврат

Разметка есть один в один: d:/серч/webapp/src/dialog-screens.tsx, RefundDialog (стр. 180–195) — кадр является копией `gJdnV`, на который код уже ссылается. Виден по адресу /dialogs (стенд, все восемь диалогов колонкой). Не хватает всплытия: в прозвоне d:/серч/webapp/src/call-mode-screen.tsx:758–770 кнопка «Брак, вернуть 199 ₽» уводит на /balance/refunds, а в раскрытой карточке d:/серч/webapp/src/object-card-disclosed-screen.tsx:318–324 исход «Брак» молча возвращает 199 ₽ без единого окна. Окно поверх экрана не открывает ничто.

### `C2B6w` СОСТОЯНИЕ · Сменить дневной лимит

Выбор лимита достижим на компьютере, но не окном: d:/серч/webapp/src/agency-staff-screen.tsx:429–441 — строка «Дневной лимит раскрытий» с чипами «5 в сутки · 25 в сутки · Без лимита» прямо в карточке сотрудника (/agency/staff/person), причём чип переключается только в памяти экрана (RightRow, стр. 264). Не хватает самого окна: заголовка «Дневной лимит · <имя>», объясняющего текста и сноски «Без лимита стоит снимать осознанно». На телефоне хуже: d:/серч/webapp/src/mobile-agency-people-screens.tsx:333–340 — кнопка «Изменить лимит» несёт только data-action и не открывает ничего.

### `a9lIk` СОСТОЯНИЕ · Сменить статус

Смена статуса достижима только поштучно и без окна: чипы «В работе · Прозвонен · Отказ» в панели фиксации звонка d:/серч/webapp/src/call-mode-screen.tsx:635–655 (/call). Кнопка «Статус» в карточке (d:/серч/webapp/src/object-card-parts.tsx:194–199) уводит туда же, клавиша S в выдаче (search-screen.tsx:539) вообще только пишет след в data-last-action. Не хватает самого кадра: окна массовой смены статуса у 12 объектов со списком статусов и сноской «Стоп-лист руками не ставится: он приходит от собственника через serch.ru/stop». На телефоне пункт «Сменить статус» (mobile-search-extra-screens.tsx:961) мёртвый.

### `c5I2qO` СОСТОЯНИЕ · В подборку

Окно выбора подборки существует и всплывает по-настоящему: d:/серч/webapp/src/features/workspace/CollectionPicker.tsx — открывается клавишей B и кнопкой «В подборку» из выдачи (search-screen.tsx:560–563) и из карточки объекта (object-card-parts.tsx:188–193, 218). Не хватает массовости: окно кладёт ОДИН объект по адресу, выбора 12 объектов на компьютере нет вовсе (в search-screen.tsx нет ни отметок строк, ни панели выбора), заголовок «Добавить 12 объектов в подборку» и фраза про «объекты добавятся все сразу» не собраны. На телефоне пункт «В подборку» (mobile-search-extra-screens.tsx:960) — только data-action.

### `nGIqH` СОСТОЯНИЕ · Новая подборка и доступ

Разметка есть один в один: d:/серч/webapp/src/dialog-screens.tsx, ClientLinkDialog (стр. 197–212) — кадр является копией `nmULz`, на который код ссылается; виден по адресу /dialogs. Не хватает всплытия: в продукте ссылка создаётся без всякого окна — d:/серч/webapp/src/collections-screens.tsx:271–283 («Создать ссылку» в таблице) и :383–388 (кнопка в подборке) сразу зовут setCollectionLink, срок «7 дней» и след «каждое открытие видно в журнале» человеку не показываются. На телефоне выбор доступа встроен в лист новой подборки (mobile-collections-screens.tsx:404–455), это другой кадр (`nVXGr`).

### `fGEdr` СОСТОЯНИЕ · Подборка отключена

Состояние достижимо, но страница другая. На телефоне работает по-настоящему: d:/серч/webapp/src/mobile-collections-screens.tsx:639–651 — при выключенной ссылке /m/collections/client показывает «Подборка не открывается · Доступ по этой ссылке закрыт». На компьютере есть d:/серч/webapp/src/collections-screens.tsx:671–694 (CollectionOffPage) с текстом «Подборка больше не публикуется», но она живёт только на стенде /screen/collection-off, который по routes.tsx:684–705 существует лишь в разработке — продуктового адреса публичной подборки на компьютере нет. Не хватает нарисованного: шапки публичной страницы с подписью «подборка» и строкой оператора ПДн, заголовка «Подборка закрыта», кнопки «Что такое „Сёрчь“», правовой строки и блока «ПОЧЕМУ ССЫЛКА ПЕРЕСТАЛА РАБОТАТЬ» из трёх объяснений плюс строки про hello@serch.ru.

### `Bnv6A` МОБАЙЛ · Состояния

Из трёх панелей собрана одна. «Пусто» есть: D:/серч/webapp/src/mobile-today-screens.tsx:314-319 рисует MobileEmptyState на маршруте /m/today (routes.tsx:547) — но заголовок «Смена не начата» вместо «Сегодня свободно» и нет кнопки «Открыть поиск». «Нет связи» нет вовсе: во всём webapp/src нет ни navigator.onLine, ни offline-состояния. «Баланс 0» нет: /m/balance при нуле показывает «0 ₽ · хватит на 0 раскрытий» и «Списаний ещё не было» (mobile-balance-screens.tsx:394-406), блокирующего состояния «Раскрытия остановлены у всего агентства» с кнопкой «Написать руководителю» не существует; на телефоне ответ no-money из disclose вообще не обрабатывается (mobile-object-screens.tsx:495).

### `cFXrK` МОБАЙЛ · Сегодня, смена не начата

Состояние достижимо: D:/серч/webapp/src/mobile-today-screens.tsx:314-319 на маршруте /m/today (routes.tsx:547) — при живом сеансе useOwnAgency() всегда истинно (demo-session.ts:463-483), так что пустой день это единственное состояние экрана. Не хватает трёх вещей из кадра: кнопки «Показать новые за ночь», чисел («девятнадцать новых за ночь», «три вчерашних раскрытых ещё не прозвонены») и самого текста — в коде заголовок «Смена не начата» и другой абзац. Полный текст кадра лежит на десктопном стенде states-screen.tsx:371-386 (кадр cTCZL), но это не телефон.

### `g4nnt` МОБАЙЛ · Подборка отключена

Состояние достижимо: /m/collections/client при выключенной или отсутствующей ссылке показывает «Подборка не открывается» (webapp/src/mobile-collections-screens.tsx:639–651). Не хватает почти всего кадра: заголовка «Подборка закрыта», подписи «подборка» в шапке, кнопки «Что такое „Сёрчь“», блока «ПОЧЕМУ ССЫЛКА ПЕРЕСТАЛА РАБОТАТЬ» из трёх сроков (доступ закрыт / что делать / про ваши данные) и нижней сноски про сервис для агентств. Сейчас это два абзаца по центру, а не страница.

### `FKB6O` ПРОФИЛЬ · Уведомления

Отдельного экрана настройки уведомлений на десктопе нет. Есть только колонка «Уведомления» внутри вкладки «Личные данные»: D:/серч/webapp/src/profile-security-screen.tsx (ProfilePage), адрес /profile. Там три строки-факта только для чтения («письмом, раз в час» и т. п.) и подпись «Настройка уведомлений появится вместе с почтовой рассылкой». Не хватает всего, что нарисовано: блока «КУДА ПРИСЫЛАТЬ» (Telegram / телефон для SMS / почта, «Проверить канал»), «ТИХИЕ ЧАСЫ» с 21:00–09:00, семи настраиваемых строк «О ЧЁМ ПРИСЫЛАТЬ» с обоймами (Сразу / Раз в час / Утром и т. д.), кнопки «Отключить всё» в шапке. Полная версия собрана только на телефоне — /m/notifications, D:/серч/webapp/src/mobile-more-screens.tsx, и это другой кадр (jTLZb).

### `vSKGt` ВХОД · Регистрация, почта занята и слабый пароль

D:/серч/webapp/src/auth-screens.tsx: живой /register (RegisterPage) при занятой почте показывает над полями «Агентство с такой почтой уже заведено. Войдите или восстановите пароль», а при коротком пароле — общую фразу «Агентство не создалось. Проверьте почту и пароль — пароль не короче десяти знаков». Статический показ ошибок стоит на /register/error (RegisterErrorPage). Не хватает двух вещей из кадра: ошибка «Пароль короче 10 знаков. Добавьте ещё четыре» не привязана к полю пароля (там только подсказка), а живой отказ сваливает почту и пароль в одну строку над формой вместо двух подписей под полями. К тому же на /register/error нарисована другая пара ошибок — ИНН + почта, а не почта + пароль.

### `G8Lhg` КАБИНЕТ · Поиск → Выдача, аренда

Режим аренды работает: D:/серч/webapp/src/search-screen.tsx, адрес /search, обойма «Продажа | Аренда» в шапке колонки фильтров (D:/серч/webapp/src/features/listings/FilterPanel.tsx), 212 объектов из D:/серч/webapp/src/data/rentals.ts через rental-rows.ts, цена без единицы при подписи «Цена, ₽/мес», «мало данных» вместо отклонения при меньше чем восьми аналогах, единица ₽/мес в шапке результатов. Не собрана арендная колонка фильтров: нет групп «ТИП ОБЪЕКТА», «СРОК» (длительно / посуточно), «УСЛОВИЯ» (без комиссии, без залога, можно с детьми, с животными), «СВЕЖЕСТЬ», а «КОМНАТ» идёт продажными чипами 1-к…4-к без «Студия» и «4+». В аренде колонка остаётся продажной — «Этаж», «Метро», «Ещё фильтры».

## Не собраны

Считать их 56 отдельными экранами нельзя: это примерно тридцать разных
вещей, у каждой кадр для компьютера и кадр для телефона.

- `pZQYx` СОСТОЯНИЕ · Нет прав
- `RYNP4` СОСТОЯНИЕ · Нет связи
- `JsVqc` СОСТОЯНИЕ · Сеанс истёк
- `Y7IyON` СОСТОЯНИЕ · Объект снят с продажи
- `V5ZdL` СОСТОЯНИЕ · Дубли объекта, откуда цена
- `Rrwq8` СОСТОЯНИЕ · Загрузка таблицы
- `rg0mY` СОСТОЯНИЕ · Поиск по номеру телефона
- `LFBew` СОСТОЯНИЕ · Заявка на возврат отправлена
- `ZmKkW` СОСТОЯНИЕ · Баланс пополнен
- `V9TDoV` СОСТОЯНИЕ · Запросить пополнение
- `d0BrE` СОСТОЯНИЕ · Платёж не прошёл
- `ISKdX` СОСТОЯНИЕ · Подписка не продлена
- `uzDok` СОСТОЯНИЕ · Лимит возвратов исчерпан
- `ALSnR` СОСТОЯНИЕ · Дневной лимит исчерпан
- `JTa1x` СОСТОЯНИЕ · Отключить сотрудника
- `jUJgJ` СОСТОЯНИЕ · Назначить объект агенту
- `WCDB0` СОСТОЯНИЕ · Центр уведомлений
- `KjWAe` СОСТОЯНИЕ · Ссылка приглашения истекла
- `f6btb` МОБАЙЛ · Поиск, первый вход
- `MN5PH` МОБАЙЛ · Поиск, перефильтровали
- `xxo1q` МОБАЙЛ · Поиск, ошибка источника
- `Jc6hY` МОБАЙЛ · Поиск, без результата
- `h33cyp` МОБАЙЛ · Сеанс истёк
- `OjZtb` МОБАЙЛ · Нет прав
- `OPyW3` МОБАЙЛ · Контакт отозван
- `b9LMVr` МОБАЙЛ · Стоп-лист
- `r3thk` МОБАЙЛ · Нет связи
- `EdWNY` МОБАЙЛ · Дубли объекта, откуда цена
- `Xea2j` МОБАЙЛ · Объект снят с продажи
- `GIv2U` МОБАЙЛ · Отключить сотрудника
- `ZQvsE` МОБАЙЛ · Назначить объект агенту
- `HRED4` МОБАЙЛ · Дневной лимит исчерпан
- `mCblr` МОБАЙЛ · Сменить дневной лимит
- `PkIhp` МОБАЙЛ · Запросить пополнение
- `s4xQF` МОБАЙЛ · Платёж не прошёл
- `oY6jq` МОБАЙЛ · Подписка не продлена
- `exztG` МОБАЙЛ · Лимит возвратов исчерпан
- `hnnq8` МОБАЙЛ · Удалить агентство
- `iWj2M` МОБАЙЛ · Передать роль
- `TIJHR` МОБАЙЛ · Реквизиты агентства
- `m0ATRJ` МОБАЙЛ · Заявка на возврат отправлена
- `l9WNps` МОБАЙЛ · Баланс пополнен
- `OWHVQ` МОБАЙЛ · Сменить статус
- `u2WQZ` МОБАЙЛ · В подборку
- `xCg0B` МОБАЙЛ · Ссылка приглашения истекла
- `l94q2r` ВХОД · Письмо для восстановления отправлено
- `EA0qM` ВХОД · Ссылка перестала работать
- `w5zQd` КАБИНЕТ · Карточка объекта, аренда — до раскрытия
- `m4NrE` КАБИНЕТ · Карточка объекта, аренда — мало аналогов
- `OKo5j` КАБИНЕТ · Похожие на Ленскую ул., 10
- `vnz7N` САЙТ · Номер исключён, подтверждение
- `cJ3Md` МОБАЙЛ · Номер исключён
- `D2UexF` МОБАЙЛ · serch.ru/stop, номер уже исключён
- `n1zw2T` ПУБЛИЧНОЕ · serch.ru/data
- `Ka2XI` ПУБЛИЧНОЕ · Откуда номер, ответ
- `mSLXH` МОБАЙЛ · Откуда номер, ответ

## Геометрия: 57 расхождений

Три корня закрыты 07.08.2026 и ниже не повторяются: раздавленная строка
выдачи (`shrink-0`), распорка строки в корне документа (`line-height: 0`),
кнопка выше своей строки (`min-h`). Остальное — ниже.

**Осторожно:** часть находок спорит с замерами, записанными в шапках самих
компонентов. Там, где кадр и старый замер расходятся, нужен новый замер
из открытого Pencil, а не выбор на слух.

### /search

**наезд, обе.** Строка выдачи `[data-slot="listing-row"]` сжата flex-контейнером. Токен `--row-obj` считается верно (88 px в просторном, 64 в плотном), но строка — flex-элемент БЕЗ `shrink-0` внутри `.list-in` (`flex flex-1 overflow-hidden`, search-screen.tsx:789), и её давит до min-content. Внутренний блок «Объект» 54 px при строке 55–56 → воздуха сверху и снизу по 0,5–1 px вместо нарисованных 21 (просторно) и 9 (плотно). Адрес и мета-строка упираются в разделитель соседней строки — это и есть «текст слипается и выходит из блока»

- замерено: первая строка 56,0 px, все последующие 55,0 px — одинаково в просторном и плотном, на 1440 и на 1280. Блок «Объект» 382×54 (просторно) и 398×54 (плотно), поле сверху 1 px, снизу 0
- в кадре: ghwPj/jzwFX — строка 908×88, «Объект» 384×46 на y=21 (поля 21/21). ZOB5K/kvqzl — строка 916×64, «Объект» 400×46 на y=9 (поля 9/9)

**обрезка, обе.** Список выдачи обрезан и НЕ прокручивается: видно 15 строк из 53, остальные 38 недоступны ничем. `.list-in` стоит `overflow: hidden`, а внешний `main[data-slot="results"]` с `overflow-y-auto` сам никуда не прокручивается, потому что список забирает остаток высоты через `flex-1`. Проверено прокруткой колеса над списком: 25 прокруток по 400 px — верх последней строки как был 3033, так и остался, все три scrollTop равны 0

- замерено: `.list-in` scrollHeight 2915 при clientHeight 836 (1440 просторно) / 856 (плотно) / 780 (1280 просторно) / 802 (1280 плотно) → за краем 2059–2135 px. `main[results]` scrollHeight − clientHeight = 0. Полностью видно 15 строк на 1440 и 14 на 1280 из 53
- в кадре: ghwPj/m9EJm — список 908×836, в нём 9 строк по 88 (792), всё внутри. ZOB5K/xiPIn — 916×856, 9 строк по 64 (576). E5kwP/Rwcsw — 1016×802. Список в кадре не обрезает содержимое

**выход за блок, просторно.** Колонка фильтров не помещается в отведённые 968 px, и главное действие «Сохранить поиск» уезжает за нижний край окна. Причина — накопленный рост колонки: плашка «Только собственники» 88 вместо 52 и блок действий столбиком 72 вместо ряда 36

- замерено: `[data-slot="filter-panel"]` scrollHeight 1040 при clientHeight 968 → 72 px за краем. Низ кнопки «Сохранить поиск» на y=1050 при нижней границе панели 1024
- в кадре: ghwPj/rkxXB — «Действия колонки» 232×36 на y=918, низ 954, поле низа 14 → вся колонка укладывается в 968 без прокрутки

**размер, обе.** Блок действий внизу колонки фильтров собран столбиком, а не рядом: кнопка «Сохранить поиск» растянута на всю ширину и стоит над «Сбросить N». В коде это названное расхождение (FilterPanel.tsx:311–331 — лестница контролов против кадра), но именно оно даёт лишние 36 px и выталкивает кнопку за край в просторном режиме

- замерено: обёртка 231×72 (flex-col, gap 8): кнопка 231×40 на y=1010, «Сбросить 4» 64,2×24 на y=1058. В плотном 235×68: кнопка 235×36, «Сбросить» под ней
- в кадре: ghwPj/MmPYN — 232×36 одной строкой: «Сохранить поиск» 156×36 на x=0, «Сбросить 7» 64×16 на x=168. C4zkJ/iBMUY — 236×36: кнопка 160×36, «Сбросить» на x=172

**выход за блок, просторно.** Метка «ФИЛЬТРЫ» в шапке колонки выходит за свою клетку: у неё `min-w-0` и распорка рядом схлопнута в ноль, поэтому текст рисуется шире выделенного ему места. Визуально пока не сталкивается с обоймой «Продажа | Аренда», но запаса нет вовсе

- замерено: span `[data-slot="typography"]` в `[filter-panel]`: scrollWidth − clientWidth = 3 px при overflow-x visible; клетка 54,2 px при нужных 57,2. Распорка между меткой и обоймой 0,0 px. В плотном 57,4 при распорке 0,8 px — не вылезает, но впритык
- в кадре: ghwPj/g5O2G7 — метка «ФИЛЬТРЫ» 58×16 на x=0, обойма «Режим» 152×28 на x=68 при ширине колонки 232 → между ними 10 px, свободного места 22 px

**размер, обе.** Вторая строка блока «Объект» (свежесть и мета) высотой 24 вместо 16: обёртка `div.min-w-0.flex-1` вокруг мета-текста наследует line-height 24 от строки выдачи, хотя сам текст 12/500 с line-height 16. Из-за этого весь блок «Объект» 54 вместо 46 — это и есть те 8 px, которые дожимают сплющенную строку

- замерено: `[listing-row] > div:first-child > div:nth-child(2)` — 382×24 (просторно) и 398×24 (плотно); текст внутри 12px/500 lh=16px, обёртка lh=24px. Блок «Объект» 54 в обеих плотностях
- в кадре: jsW77/XqGtw — «Строка 2» 384×16 на y=30, тексты 12/500 при lh 1,3333 = 16. Блок «Объект» 46

### /searches

**кегль, обе.** Заголовок первого входа «С чего начать поиск» набран двадцатым кеглем — тем же, что и название раздела «Сохранённые поиски» строкой выше. В кадре он крупнее названия раздела и держит иерархию экрана

- замерено: h2 `[data-slot="typography"]` — 20px/600, высота строки 28 px
- в кадре: tkltB/udWJt — «С чего начать поиск» 28/600, высота текста 32

**размер, обе.** Плашка «ТРИ РАЗНЫЕ ВЕЩИ, И ВСЕ НАЗЫВАЮТСЯ ПОИСКОМ» растянута на всю ширину контента вместо колонки 740. Строки объяснений тянутся на 1104 px — это 150+ знаков в строку, читать такое нечем

- замерено: `[data-slot="three-things"]` — 1152×164 на x=264 y=516, поля 20/24, строки внутри 1104 шириной
- в кадре: tkltB/UevZX — 740×156 на x=264 y=612, поле 20, строки внутри 700

**размер, обе.** Готовые наборы отданы чипами в один ряд вместо карточек с подписью. Карточка в кадре несёт название и вторую строку («148 объектов от собственников»); в продукте осталось только название

- замерено: ряд `div.flex.w-full.flex-wrap.items-center.gap-2` — 1152×32 на y=344, четыре чипа высотой 32
- в кадре: tkltB/dDQLH — «Ряд пресетов» 1152×76 на y=344: четыре карточки 270×76 с шагом 294, название 14/600 на y=16, подпись 12/500 на y=44

**размер, обе.** Кнопка «Найти» в строке первого поиска взята с верхней ступени лестницы (48) вместо нарисованной 32, и поле ввода рядом уже кадра на 40 px

- замерено: `[data-slot="button"]` — 118,7×48 на x=876 y=236. Поле ввода 600×48 на x=264 (подчёркивание 264…864)
- в кадре: tkltB/uY46B — «Строка поиска» 740×48: подчёркивание поля идёт до x=904, «Кнопка Найти» 88×32 на x=916 y=248

**отступ, обе.** Зазор от строки заголовка раздела до блока «С чего начать поиск» — 32 px вместо 24: страница кабинета задаёт общий `gap-8`, а на этом экране кадр держит шаг 24 между всеми блоками подряд

- замерено: строка заголовка 264,80 … 112; блок `[data-slot="first-search"]` начинается на y=144 → зазор 32 px. `[data-slot="cabinet-page"]` gap=32px
- в кадре: tkltB — «Заголовок» 24…56, «Заголовок начала» на 80 → 24; и дальше все зазоры между блоками ровно 24 (80+80=160 → 184, 184+48=232 → 256, 256+108=364 → 388, 388+144=532 → 556)

### /search, /searches

**размер, обе.** Пункт бокового меню (общая часть кабинета, видна на обоих адресах) 40 px вместо 36, шаг 42 вместо 38. Причина та же, что и в строке выдачи: текст внутри занимает 24 при поле 8, вместо 20 при поле 8. К пятому пункту накапливается 16 px, и весь список меню съезжает вниз

- замерено: `[data-slot="nav-item"]` — 215×40, поля 8/12, иконка 16 на y=80 при верхе пункта 68 (то есть 12 сверху). Пункты на y=68, 110, 152, 194, 236
- в кадре: BVT4W и все экземпляры в ghwPj, ZOB5K, bZOeU, tkltB, E5kwP — 216×36, иконка 16 на y=10 (поле 10). Пункты в кадре на y=68, 106, 144, 182, 220

### /call

**наезд, обе.** Подпись главной кнопки подвала «Сохранить и к следующему» не помещается в кнопку и наезжает на значок горячей клавиши ⏎. Узел: div[data-slot="fix-panel"] > div.flex.h-12 > div.flex.min-w-0.flex-1 > button[data-slot="button"] > span[data-slot="typography"] (класс whitespace-nowrap, overflow: visible — текст не обрезается, а выезжает поверх соседа). Причина числом: кнопка получает поля px-4 = 16 и compact:px-8 = 32, из-за чего подписи остаётся меньше места, чем нужно тексту. На снимке в просторном «у» вплотную упирается в ⏎, в плотном ⏎ полностью закрыт текстом.

- замерено: просторно: коробка подписи clientWidth 194 при scrollWidth 203 → вылет 9 px, текст доходит до x=1221, значок ⏎ стоит на x=1220 (наложение). плотно: clientWidth 170 при scrollWidth 203 → вылет 33 px, текст доходит до x=1237, ⏎ на x=1212..1221 — закрыт целиком. Кнопка 245×48 (просторно) и 253×44 (плотно).
- в кадре: pR6T1 → Подвал фиксации → «Сохранить и к следующему»: кнопка 247×48, «Текст сохранить» 204 px при 16/600 на x=12, зазор до «Хоткей сохранить» 8, сам ⏎ 12 px на x=224, справа остаётся 11. Текст в кадре помещается целиком, наложения нет.

**размер, обе.** Правовая строка под кнопками звонка («Звони по объекту на Ленской, 10. Разговор о других услугах…») набрана с межстрочным 24 вместо 16 и занимает блок вдвое выше нарисованного. Узел: div.w-140.shrink-0 > span[data-slot="typography"].text-xs. Сам span имеет font 12px/16px, но лежит прямым текстом в блоке без своего leading, и высоту строки задаёт распорка родителя (16px/24px). Из-за этого весь низ колонки «Объект» съезжает вниз на 16 px. Тот же текст на /object/disclosed набран правильно и даёт ровно 32 — сравнение подтверждает, что дефект локальный.

- замерено: блок 560×48, сам текст 538×39 (две строки с шагом 24). Следующий блок «КАК ЗВОНИТЬ» начинается на y=762 вместо 746.
- в кадре: pR6T1 → «Рамка разговора»: текст 560×32, lineHeight 1.3333 (12 × 1.3333 = 16), две строки по 16. Следующий блок «Как звонить» на y=746.

**размер, обе.** Строки блока «КАК ЗВОНИТЬ» выше нарисованных на 4 px каждая — по той же причине, что и правовая строка: ячейки div.w-35.shrink-0 и div.min-w-0.flex-1 не имеют своего leading, и распорка родителя 16px/24px растягивает строку до 24, хотя в ней только текст 12/16 и 13/20. Четыре строки дают +16 к блоку, и блок в сумме уезжает на 32 от нарисованного места.

- замерено: строка скрипта 924×24 (все четыре), «Реплики» 924×132, блок «КАК ЗВОНИТЬ» 924×164 на y=762.
- в кадре: pR6T1 → «Как звонить» 924×148 на y=746; «Реплики» 924×116; каждая «Строка скрипта» 924×20, зазор между строками 12.

**размер, обе.** Строка «Брак, вернуть 199 ₽» внизу панели фиксации ниже нарисованной и лишилась шеврона раскрытия. Узел: button.flex.w-full.items-center.gap-2 в div[data-slot="fix-panel"] — в нём только две подписи, распорки и иконки нет, поэтому «четыре причины» висит сразу за заголовком, а правый край строки пустой.

- замерено: строка 419×20, два потомка: span «Брак, вернуть 199 ₽» (120×20) и span «четыре причины» (91×16). Иконки нет.
- в кадре: pR6T1 → «Брак» 420×32: «Заголовок брака» 119×20 на y=6, «Подпись брака» 92×16 на x=127, «Распорка брака» 169 px на x=227, «Раскрыть брак» — иконка 16×16 на x=404.

### /object

**отступ, обе.** Строка «Источник данных» — боковые поля вдвое меньше нарисованных и форма другая. Узел: div[data-action="раскрыть состав полей источника данных"] (px-3.5 = 14, rounded-lg = 10). Из-за этого подпись «шесть полей по ст. 18 ч. 3 152-ФЗ» и шеврон стоят на 13–14 px ближе к краям, чем в кадре, и строка читается как прямоугольная плашка, а не как обведённая капсула.

- замерено: поля слева и справа 14 px (текст начинается на x=15 от левого края блока 564, шеврон кончается на 549 — справа 15), радиус 10 px, высота 40, зазор 10.
- в кадре: Fo8gk → «Источник данных»: w=564 h=40, padding [0, 28], cornerRadius 999 (капсула), gap 10. «Название источника» на x=28, «Раскрыть источник» 16×16 на x=520 (справа 28).

**отступ, обе.** В блоке «СРОК В ВЫДАЧЕ И ИСТОРИЯ ЦЕНЫ» (вторая колонка доказательств) два ритма больше нарисованных. Внутренняя колонка задана как flex-col gap-2 (8) и в неё дополнительно вставлены распорки div.h-2 — зазор складывается втрое: 8 + 8 + 8 = 24. Из-за этого шкала срока и мини-таблица разъезжаются, «честная строка» отрывается от таблицы, и вся колонка становится на 24 px выше нарисованной; полоса из трёх колонок вырастает с 236 до 260.

- замерено: от шкалы срока до мини-таблицы 24 px (шкала кончается на y=596, таблица начинается на y=620); от мини-таблицы до честной строки 24 px (таблица кончается на y=734, текст начинается на y=758); блок 368×260, полоса трёх колонок 1152×260.
- в кадре: Fo8gk → Колонка 2 → «Блок СРОК В ВЫДАЧЕ И ИСТОРИЯ ЦЕНЫ» 368×236: «Срок» кончается на y=66, «История цены» начинается на y=82 → 16; внутри истории «Ритм до честной строки» 8 (таблица кончается на y=114, «Честная строка» на y=122).

### /object/disclosed

**отступ, обе.** Та же строка «Источник данных» на карточке после раскрытия — тот же общий компонент CardSourceRow, тот же дефект: поля 14 вместо 28, радиус 10 вместо капсулы.

- замерено: поля слева и справа 14 px, радиус 10 px, высота 40; «Источник данных» на x=15, шеврон кончается на 549.
- в кадре: NKj5L → «Источник данных»: w=564 h=40, padding [0, 28], cornerRadius 999, «Название источника» на x=28, иконка на x=520.

**отступ, обе.** Тот же блок «СРОК В ВЫДАЧЕ И ИСТОРИЯ ЦЕНЫ», здесь он стоит третьей колонкой — те же два сложенных ритма по 24 вместо 16 и 8 (общий компонент AgeAndPriceBlock).

- замерено: от шкалы срока до мини-таблицы 24 px, от мини-таблицы до честной строки 24 px; блок 368×260.
- в кадре: NKj5L → Колонка 3 → «Блок СРОК В ВЫДАЧЕ И ИСТОРИЯ ЦЕНЫ» 368×236; зазор до истории цены 16, ритм до честной строки 8.

**размер, просторно.** Кнопка «Раскрыть · 199 ₽» в блоке «ПОХОЖИЕ ОБЪЕКТЫ» шире нарисованной на 16 px из-за полей px-6 = 24 вместо 16. Кнопка живёт в строке с flex-1 у адресной колонки, поэтому лишние 16 отбираются у адреса: колонка адреса сжимается с 664 до 648. То же на третьей строке с «Открыть · 0 ₽» (136 вместо 120, колонка 668 вместо 684). В плотном режиме кнопка ровно 140 — расходится только просторный.

- замерено: button[data-slot="button"].h-ctl-sm 156×32, поля 24 слева и справа, подпись 106 px при 14/600; колонка адреса div.min-w-0.flex-1 = 648. Третья строка: кнопка 136×32, колонка 668.
- в кадре: NKj5L → «Похожий Партизанская ул., 15» → «Действие» 140×32, padding [0, 16], «Текст» 108 px на x=16; «Объект» 664. Третья строка: «Действие» 120×32, «Объект» 684.

**кегль, просторно.** Кнопка «Скопировать» в строке номера набрана на ступень крупнее нарисованной: подпись 14/600 вместо 13/600, поля 24 вместо 20, зазор до иконки 8 вместо 6, высота 32 вместо 28. Итог — кнопка на 18 px шире кадра. В плотном режиме та же кнопка выходит 143×28 с подписью 13/600, то есть почти по кадру, — расходится именно просторный. Замечу конфликт: лестница контролов проекта начинается с 32, а кадр рисует 28, поэтому высоту решать владельцу; ширина, поля и кегль расходятся однозначно.

- замерено: button[data-slot="button"].h-ctl-sm.px-6 157×32, иконка 14, зазор 8, подпись «Скопировать» 85 px при 14/600.
- в кадре: NKj5L → «Номер» → «Скопировать» 139×28, padding [0, 20], gap 6, «Иконка копии» 14×14 на x=20, «Текст копии» 79 px при 13/600 на x=40.

### /balance, /balance/refunds, /balance/top-ups, /balance/documents

**выход за блок, просторно.** Шапка раздела — первый ребёнок [data-slot=cabinet-page], div.flex.h-7 — высотой 28 px, а три кнопки внутри неё («Скачать акт», «Скачать счёт», «Пополнить») высотой 32 px. Кнопки торчат из полосы на 2 px сверху и на 2 px снизу. Низкая шапка поднимает весь экран на 4 px: ключевые цифры, вкладки и плашка стоят выше нарисованного.

- замерено: полоса 1152×28 @264,80; scrollHeight 30 при clientHeight 28; кнопки занимают y 78–110 при полосе 80–108; ключевые цифры y=132, вкладки y=242, плашка y=302
- в кадре: кадр w0qrBv (и NanKI, ruMMX, uz1ET): Заголовок 1152×32, y 80–112; Ключевые цифры y=136, Вкладки y=246, Лимит возвратов y=306

**отступ, обе.** Пояснение в тёплой плашке прижато к правому краю распоркой div.h-px.flex-1 внутри [data-slot=notice-bar], а в кадре оно идёт сразу за правилом через зазор 12. Посреди плашки остаётся пустая дыра до 419 px.

- замерено: /balance/documents пояснение на x=1064; /balance/top-ups — x=1067; /balance — x=974
- в кадре: кадр uz1ET — x=645; ruMMX — x=732; w0qrBv — x=764 (пояснение через 12 px после правила)

**отступ, обе.** У тёплой плашки [data-slot=notice-bar] боковые поля px-4, то есть 16 px, а в кадрах баланса «Лимит возвратов» нарисован с полями 32 px. Текст правила начинается на 16 px левее.

- замерено: padding-left/right 16 px, текст правила начинается на x=280
- в кадре: кадры w0qrBv, NanKI, ruMMX, uz1ET: pad=[0,32], текст правила на x=296

**размер, обе.** Тёплая плашка [data-slot=notice-bar] скруглена по rounded-lg, а в кадрах баланса она пилюля. На высоте 44 разница радиуса 12 px.

- замерено: border-radius 10 px
- в кадре: кадры w0qrBv, NanKI, ruMMX, uz1ET: cornerRadius 999 — пилюля, то есть 22 px при высоте 44

**отступ, обе.** Разделитель ряда ключевых цифр лежит внутри первой колонки w-75, поэтому текстовая колонка остатка сжата до 271, а три правые метрики уехали влево на 29 px. Последняя метрика на 29 px шире соседей.

- замерено: колонка остатка 271 px; разделители x=563, 847, 1131; метрики 2–4 начинаются на x=592, 876, 1160; четвёртая метрика шириной 256
- в кадре: кадр w0qrBv: Метрика остаток 300 px; Делители x=592, 876, 1160; метрики 2–4 на x=621, 905, 1189; каждая шириной 227

**размер, просторно.** Тихие кнопки «Скачать акт» и «Скачать счёт» на просторной плотности на ступень крупнее нарисованных: 32 вместо 28 в высоту, поля 24 вместо 20, кегль 14 вместо 13. В плотном режиме продукт выдаёт ровно кадровые 114×28 и 122×28 с полями 20 — то есть кадровый размер стоит не на той плотности.

- замерено: «Скачать акт» 128×32 @1008,78, padding 24; «Скачать счёт» 136×32 @1148,78, padding 24
- в кадре: кадр w0qrBv: Скачать акт 113×28 @1056,82, pad=[0,20], текст 13/600; Скачать счёт 120×28, pad=[0,20], текст 13/600

### /balance/refunds

**размер, обе.** В плашке лимита возвратов правило и пояснение не помещаются в одну строку и переносятся на две, плашка вырастает с 44 до 64. Правилу достаётся 377 px, хотя строка «Лимит возвратов по субъективным причинам: 0 из 12 за 30 дней» рассчитана в кадре на 385 px в одну строку.

- замерено: плашка 1152×64 @264,302; правило 377×40 (две строки); пояснение 547×32 (две строки)
- в кадре: кадр NanKI: Лимит возвратов 1152×44; Текст лимита 385×20 в одну строку; Пояснение лимита 563×16 в одну строку

### /balance, /balance/documents

**размер, обе.** В нижнем блоке правил («КАК СЧИТАЮТСЯ ДЕНЬГИ», «ЕСЛИ ДОКУМЕНТА НЕ ХВАТАЕТ») разделитель тоже стоит внутри колонки flex-1, из-за чего колонки получаются неравными. На /balance/top-ups тот же блок собран иначе — разделитель вынесен отдельным mx-6 — и даёт ровно кадровые 351/351/351.

- замерено: /balance: колонки правил 343, 343, 368; разделители x=631 и 1023
- в кадре: кадры w0qrBv и uz1ET: колонки 351, 351, 351; разделители x=639 и 1040

### /collections

**размер, обе.** Плашка «На публичной странице телефона нет ни в каком виде» на 6 px выше нарисованной: вертикальные поля py-3 (12) вместо 9, которые даёт фиксированная высота 56 при тексте 38. Из-за этого таблица подборок опускается на 6 px.

- замерено: плашка 1152×62 @264,136; таблица подборок начинается на y=222
- в кадре: кадр o8RcIC: Плашка о телефоне 1152×56; Таблица подборок на y=216

### /today, /collections, /balance, /balance/refunds, /balance/top-ups, /balance/documents

**отступ, просторно.** Главная кнопка в шапке раздела набрана с боковыми полями 24 px вместо 16 и оттого шире нарисованной на 17–18 px. Компонент кадра «C Кнопка · 32» (w2pux) задаёт pad=[0,16]; поля 24 в кадрах есть только у кнопки «Пополнить» в верхней шапке кабинета, и там продукт совпадает.

- замерено: /today «Прозвон» 105×32 @1311,80; /collections «Новая подборка» 155×32 @1261,80; /balance «Пополнить» 120×32 @1296,78 — везде padding 24
- в кадре: кадр kNd9b: Кнопка прозвона 88×32 @1328,80, pad=[0,16]; кадр o8RcIC: Новая подборка 137×32 @1279,80, pad=[0,16]; кадр w0qrBv: Пополнить 103×32, pad=[0,16]

### /today, /collections, /collections/inside, /balance, /balance/refunds, /balance/top-ups, /balance/documents

**размер, обе.** Пункты бокового меню [data-slot=nav-item] выше нарисованных на 4 px и скруглены по rounded-lg вместо пилюли. Пять пунктов подряд уводят всё, что ниже, на 23 px: метка «СОХРАНЁННЫЕ ПОИСКИ» стоит на y=311 вместо 288. Плотный режим сайдбар не сжимает — там те же 40.

- замерено: 215×40, радиус 10 px, ряды на y=68, 110, 152, 194, 236; метка «СОХРАНЁННЫЕ ПОИСКИ» на y=311
- в кадре: компонент C6b6DX и кадр kNd9b: 216×36, cornerRadius 999 (пилюля), ряды на y=68, 106, 144, 182, 220; Метка поисков на y=288

**кегль, обе.** В поле глобального поиска [data-slot=global-search] плейсхолдер и подсказка горячей клавиши набраны полужирным, а в кадре они обычного веса — поле выглядит тяжелее нарисованного.

- замерено: «Адрес, телефон или номер» — 14 px, вес 600; «⌘K» — 12 px, вес 600
- в кадре: кадр kNd9b: Плейсхолдер 14/500, Хоткей 12/500

### /agency, /agency/staff, /agency/staff/person, /agency/refusals, /agency/access, /agency/consents, /agency/settings, /profile, /profile/login-policy

**выход за блок, просторно.** Кнопка действия в строке заголовка — button[data-slot=button].h-ctl-sm.px-6 внутри строки div.flex.h-7.w-full.items-center.gap-3. Кнопка выше строки, в которой лежит, и торчит из неё сверху и снизу; строка отдаёт scrollHeight 30 при clientHeight 28.

- замерено: кнопка 32 px высотой, поля по бокам 24 px, подпись 14/600; строка 28 px, кнопка занимает y=78…110 при строке y=80…108 — по 2 px наружу с каждой стороны. В плотном режиме та же кнопка становится 28 px / поля 20 / 13-600 и из строки не вылезает.
- в кадре: Во всех кадрах группы это контрол ровно 28 px с полями [0,20] и подписью 13/600: Iebim «Экспорт» 218×28, Y2Up0t «Экспорт для проверки» 175×28, mCjJV «Выгрузить журнал» 153×28, H6amtE «Выгрузить для проверки» 189×28, cNIZP «Скачать договор» 142×28, t6b58y «Отключить доступ» 153×28, XqdvJ и v9Z5fD «Выйти из аккаунта» 151×28. Ширины в плотном режиме совпадают с кадром до 1–4 px, то есть кадровые значения продукт уже умеет — они стоят не в той плотности.

### /agency/plan

**выход за блок, обе.** Крупные числа тарифа — span[data-slot=typography].text-4xl («3 000 ₽», «1 из 20», «199 ₽»). Кегль 40 при высоте строки 42, реальный бокс глифов 45: текст выходит за свою строку вверх и вниз по 1,5 px.

- замерено: scrollHeight 45 при clientHeight 42, разница 3 px, во всех трёх колонках и в обеих плотностях; font-size 40, line-height 42 px
- в кадре: AcGYf, узел «Метка» в каждой из трёх «Колонок»: 352×42, кегль 40/600, lineHeight 1.05 — высота строки ровно 42, без запаса

**отступ, обе.** Тело экрана не вложено в контейнер «Тариф» с полем 24, и заголовок собран горизонтальной строкой вместо вертикальной шапки. Из-за этого весь экран стоит левее и выше кадра.

- замерено: строка заголовка div.flex.h-7 на x=264, y=80 (заголовок и подпись в одну строку); ряд из трёх колонок на x=264, y=132
- в кадре: AcGYf: всё содержимое внутри рамки «Тариф» 1152×670 с полем 24 и внутренним зазором 32. «Шапка» на x=288, y=104 — вертикальная, 1104×52: «Тариф и подписка» 20/600 сверху и подпись 13/500 под ним с зазором 4. «Три колонки» на x=288, y=188. Расхождение 24 px по горизонтали и 56 px по вертикали.

**размер, обе.** Блок «Двадцать первый» — абзац span[data-slot=typography].text-13 растянут на всю ширину рабочей области в одну строку, читать неудобно; заголовок блока подменён мелкой капслок-меткой.

- замерено: абзац 1152×20, одна строка, кегль 13/500; заголовок блока 11/600 капсом «ДВАДЦАТЬ ПЕРВЫЙ»
- в кадре: AcGYf «Двадцать первый»: «Текст» шириной 700 px в три строки, кегль 14/500; заголовок блока — «Больше двадцати сотрудников» 16/600 строчными, высота 24. Ширина абзаца больше кадровой на 452 px.

**отступ, обе.** Вертикальные волосяные разделители между колонками тарифа — span.absolute.left-[364px] и span.absolute.left-[740px].

- замерено: линии на 364 и 740 px от левого края ряда (ровно по середине зазоров между колонками)
- в кадре: AcGYf «Разделитель 1» на x=380 и «Разделитель 2» на x=772 внутри «Трёх колонок». Расхождение 16 и 32 px. Оговорка: в кадре эти линии заходят внутрь второй и третьей колонок (колонки начинаются на 376 и 752), так что кадр здесь сам выглядит спорно — решать главному чату.

### /agency/staff

**размер, просторно.** Строка заголовка страницы div.flex.h-7 и главная кнопка «Пригласить агента» в ней. На этом экране кадр рисует строку 32, а не 28, и кнопку с узкими полями.

- замерено: строка заголовка 28 px; кнопка 170×32 с полями 24 px, подпись 14/600; вкладки агентства начинаются на y=132 (76 от верха контента)
- в кадре: u7anli: «Заголовок» 1152×32, «Пригласить агента» 153×32 с полями [0,16], подпись 14/600, «Подразделы» на y=80 от верха контента. Строка на 4 px ниже кадра, поля кнопки на 8 px шире с каждой стороны, вкладки на 4 px выше.

### /agency/refusals

**размер, просторно.** Правовая плашка «Снять отказ из интерфейса нельзя…» — div.flex.rounded-lg.bg-warm.px-4.py-3. Высота задана полями, а не кадровым размером, поэтому плашка выше кадра и всё под ней съезжает.

- замерено: 1152×62, поля 12 сверху и снизу, 16 по бокам; содержимое внутри 38 px
- в кадре: Y2Up0t «Правовая плашка» 1152×56, поля [0,16]. Разница 6 px, на столько же ниже уезжает всё, что стоит под плашкой.

### /agency/settings, /agency/consents, /agency/staff/person, /agency/plan

**отступ, просторно.** Вертикальный зазор между блоками страницы у контейнера контента (div[data-slot=cabinet-page] / div.flex-1.flex-col.gap-6.p-6). Продукт ставит один и тот же зазор на все экраны, кадры этих четырёх экранов рисуют меньший.

- замерено: gap 24 px на всех четырёх экранах. На /agency/consents набегает: вкладки на y=132, фильтры на y=192, таблица на y=244
- в кадре: «Контент» gap=20 в кадрах cNIZP, H6amtE, t6b58y, AcGYf (в кадрах Iebim, u7anli, Y2Up0t, mCjJV, XqdvJ, v9Z5fD, B8Irc стоит именно 24 — там продукт прав). Для /agency/consents кадр H6amtE даёт вкладки на y=128, фильтры на y=184, таблицу на y=232 — расхождение 4, 8 и 12 px по нарастающей.

### /agency/invite

**отступ, обе.** Между заголовком и строкой возврата «← Сотрудники» продукт вставляет полосу вкладок агентства div[data-slot=agency-tabs], которой в кадре нет. Из-за неё всё содержимое экрана съезжает вниз.

- замерено: вкладки 1152×36 на y=132 плюс зазор 24; строка возврата на y=192, плашка про счёт на y=252, форма на y=320, блок «Как работает приглашение» на y=729
- в кадре: B8Irc: детей у «Контента» пять, полосы «Подразделы» среди них нет. Строка возврата на y=132, «Полоса про счёт» на y=192, «Форма пополнения» на y=260, «Как работает приглашение» на y=681. Всё ниже заголовка ниже кадра на 60 px.

### /profile

**размер, просторно.** Две колонки личных данных — div.flex.w-141 слева и div.flex.w-141 справа. В продукте колонки равные, в кадре узкая слева и широкая справа.

- замерено: левая 564, правая 564, зазор 24, суммарно 1152
- в кадре: XqdvJ «Колонки»: «Личные данные» 466 и «Настройки» 662 при том же зазоре 24. Расхождение 98 px в каждую сторону. Для сравнения: /profile/login-policy (кадр v9Z5fD, 564+564), /agency/settings (cNIZP, 466+662 → в продукте 468+660) и /agency/staff/person (t6b58y, 466+662 → 468+660) с кадром сходятся.

### /agency/efficiency

**экран сломан, обе.** Адреса /agency/efficiency в продукте нет: в webapp/src/routes.tsx зарегистрирован только /agency → AgencyEfficiencyPage. Открывается шаблонный NotFoundPage, который без Supabase показывает англоязычную карточку «Session check is temporarily unavailable / Your session was not cleared. Check the connection and try again.» с кнопкой «Try again» (webapp/src/components/WebRouteSections.tsx).

- замерено: видимых узлов на странице 16 против 117–191 на рабочих экранах группы; никакого продуктового содержимого, текст английский
- в кадре: Кадра под /agency/efficiency в файле нет — эффективность живёт на /agency (кадр Iebim). То есть либо адрес в задании лишний, либо любому промаху по адресу продукт отвечает англоязычной заглушкой чужого шаблона вместо русского «страница не найдена».

### /login

**размер, обе.** Обёртка подзаголовка `div.w-160` (AuthShell.tsx:113) не сбрасывает интерлиньяж: строка внутри получает строчный бокс 24 (распорка от корня 16/24), а не 20 от токена text-sm. Блок подзаголовка выше кадра, и весь ритм формы ниже уезжает.

- замерено: div.w-160 (подзаголовок): y=182 h=24; span внутри 14px/lh 20px. Блок «Поля» из-за этого y=238
- в кадре: tIplu «Подзаголовок регистрации» y=182 h=20 (14/500, lh 1.4286); «Поля» y=234

**размер, обе.** Правовая строка внизу формы (`div.w-160`, AuthShell.tsx:125) — та же распорка: две строки по 24 вместо 16. Блок вдвое выше нарисованного и начинается на 16 px выше.

- замерено: div.w-160 (правовая): y=912 h=48, текст 12px/lh 16px, 2 строки
- в кадре: tIplu «Правовая строка» y=928 h=32

**кегль, обе.** Подсказка под полем «ПАРОЛЬ» набрана ступенью 12/500 (variant metaText в AuthField), в кадре это ступень 13/600. Из-за этого поле пароля ниже нарисованного.

- замерено: span[data-slot=typography] в auth-field: fs=12px lh=16px fw=500, h=16; поле целиком h=88
- в кадре: tIplu, поле ПАРОЛЬ: подсказка 13/600, h=20; поле целиком h=92

**отступ, обе.** Зазор в ряду действий (кнопка ↔ «Ещё нет агентства?») — gap-5. Это общий ряд всех девяти экранов входа, значит расхождение повторяется на каждом.

- замерено: div.flex.w-full.items-center.gap-5: gap=20px; кнопка кончается на x=190, следующий блок с x=210
- в кадре: tIplu «Действия» gap=24 (ряд действий 0…116, строка другого пути с x=140)

### /forgot

**размер, обе.** Та же обёртка подзаголовка: строка 24 вместо 20, и весь столбец формы сдвинут вниз на 4 px.

- замерено: div.w-160 y=182 h=24; «Поля» y=238; ряд действий y=330
- в кадре: C1oCj: подзаголовок y=182 h=20; «Поля» y=234; «Действия» y=326

**размер, обе.** Правовая строка внизу формы — две строки по 24 вместо 16.

- замерено: div.w-160 (правовая) y=912 h=48
- в кадре: C1oCj «Правовая строка» y=928 h=32

### /new-password

**размер, обе.** Двустрочный подзаголовок в обёртке `div.w-160` даёт 48 вместо 40; форма ниже уезжает на 8 px.

- замерено: div.w-160 y=182 h=48 (2 строки по 24); «Поля» y=262; ряд действий y=462
- в кадре: G6S9d: подзаголовок y=182 h=40; «Поля» y=254; «Действия» y=454

**размер, обе.** Правовая строка внизу формы — две строки по 24 вместо 16.

- замерено: div.w-160 (правовая) y=912 h=48
- в кадре: G6S9d «Правовая строка» y=928 h=32

### /register

**размер, обе.** Двустрочный подзаголовок в обёртке `div.w-120` даёт 48 вместо 40; поля, согласия и кнопка уезжают вниз на 8 px.

- замерено: div.w-120 y=182 h=48; «Поля» y=262; «Согласия» y=676; ряд действий y=764
- в кадре: V3seY: подзаголовок y=182 h=40; «Поля» y=254; «Согласия» y=668; «Действия» y=756

**размер, обе.** Правовая строка (три строки в обёртке `div.w-120`) занимает 72 вместо 48 и начинается на 24 px выше нарисованного.

- замерено: div.w-120 (правовая) y=888 h=72, текст 12px/lh 16px
- в кадре: V3seY «Правовая строка» y=912 h=48

### /register/error

**размер, обе.** То же самое на экране с ошибками: подзаголовок 48 вместо 40, весь столбец формы ниже на 8 px.

- замерено: div.w-120 y=182 h=48; «Поля» y=262; «Согласия» y=708; ряд действий y=796
- в кадре: J0pPv: подзаголовок y=182 h=40; «Поля» y=254

**размер, обе.** Правовая строка внизу — 72 вместо 48.

- замерено: div.w-120 (правовая) y=888 h=72
- в кадре: J0pPv «Правовая строка» y=912 h=48
