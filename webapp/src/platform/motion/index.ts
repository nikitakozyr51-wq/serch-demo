/**
 * Движение, не привязанное ни к одному разделу продукта.
 *
 * Счётчик баланса в шапке, счётчик найденного над выдачей и счётчики вкладок —
 * это одно и то же движение, а живёт оно между тремя разными разделами.
 * Пока хук лежал внутри кабинета, взять его в выдачу можно было только через
 * зависимость «выдача знает про кабинет» — связь, которой в продукте нет.
 */
export { useAnimatedNumber } from './useAnimatedNumber'
export { usePending } from './usePending'
