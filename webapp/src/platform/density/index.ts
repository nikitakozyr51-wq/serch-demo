/**
 * Плотность интерфейса — настройка человека, а не экрана.
 *
 * Живёт в `platform`, потому что переключатель стоит на выдаче, а действует
 * на таблицы агентства, баланса и подборок. Внутри одного раздела ей места
 * нет: тогда остальные разделы узнавали бы о выборе через зависимость
 * от выдачи.
 */
export { setDensity, useDensity } from './useDensity'
export type { Density } from './useDensity'
