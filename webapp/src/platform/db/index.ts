/**
 * База продукта.
 *
 * Живёт в `platform`, а не в разделе: это транспорт, а не бизнес-правило.
 * Что именно хранится и по каким правилам — знает `features/workspace`,
 * а как это доехало до сервера — знает только этот модуль.
 */
export { db, hasDatabase } from './client'
