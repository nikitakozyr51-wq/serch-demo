/**
 * Кто владеет клавишами, пока поверх экрана открыто окно.
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * ПОЧЕМУ В ПЛАТФОРМЕ, А НЕ В КАБИНЕТЕ
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Жил в `features/cabinet` — там его завели, там он и понадобился первым.
 * Но окна открывает не только кабинет: просмотрщик кадров живёт в выдаче
 * (`features/listings`), и ему счётчик нужен ровно затем же, зачем палитре.
 *
 * Прямой путь «выдача спрашивает кабинет» закрыт, и закрыт по делу: кабинет
 * уже импортирует выдачу (`CabinetHeader`, `CommandPalette`, `MobileHeader`
 * берут из неё `groupDigits` и `plural`). Встречная стрелка замкнула бы
 * их в кольцо, и первый же разбор зависимостей на это указал бы.
 *
 * Владение клавишами вообще не свойство кабинета: это правило окна как
 * такового — «пока я открыт, экран подо мной клавиш не получает». Такие
 * правила и живут в платформе, куда смотрят все и которая не смотрит
 * ни на кого.
 */
export {
  isOverlayOpen,
  onPaletteRequest,
  requestPalette,
  resetOverlays,
  setOverlayOpen,
  useOverlayOpen,
} from './overlay-state'
