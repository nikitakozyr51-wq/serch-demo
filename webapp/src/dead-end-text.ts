/**
 * ОБЩИЕ ТУПИКИ · текст трёх состояний, один на компьютере и на телефоне.
 *
 * Снято с шести кадров: `pZQYx` / `OjZtb` (нет прав), `RYNP4` / `r3thk`
 * (нет связи), `JsVqc` / `h33cyp` (сеанс истёк).
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * ПОЧЕМУ ТЕКСТ ЖИВЁТ ОТДЕЛЬНО ОТ ЭКРАНОВ
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Кадров шесть, состояний три. Пары — не «десктоп и его сжатая копия»:
 * у окна и у листа разная форма, но человек за ними один и тот же, и ответ
 * на вопрос «что случилось» обязан быть одним. Две копии расходятся по первой
 * же правке — так уже разошлись два ответа на отключённую подборку
 * (см. `collection-off-text.ts`), и там это чинили задним числом.
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * ЧТО В КАДРАХ РАЗОШЛОСЬ И КАК ЭТО СВЕДЕНО
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * 1. **Заголовок «нет прав».** `pZQYx` — «Раздел доступен руководителю
 *    агентства», `OjZtb` — «Раздел только для руководителя». Смысл один,
 *    слова разные. Взят короткий: он говорит ровно то же, а владелец
 *    постоянно просит убрать лишний текст.
 * 2. **Заголовок «сеанс истёк».** `JsVqc` — «Сеанс завершён», `h33cyp` —
 *    «Сеанс истёк». Взято «Сеанс истёк»: так называются оба кадра, и так
 *    это состояние уже названо в `demo-session.ts`.
 * 3. **Что уцелело после выхода.** `JsVqc` обещает «раскрытые контакты,
 *    история звонков и стоп-лист», `h33cyp` — «фильтры, подборки и история».
 *    Два разных обещания об одних и тех же данных. Взят список `JsVqc`:
 *    он совпадает с тем, что на самом деле переживает выход, — работа
 *    агентства лежит в `features/workspace` и сеансу не принадлежит.
 * 4. **«Руководитель задаёт своё значение для всего агентства» — выброшено.**
 *    Это единственная фраза кадра, которая противоречит собранному продукту:
 *    время простоя в «Сёрчи» ЛИЧНОЕ. Так записано и в `DemoSession.idleMinutes`,
 *    и на экране «Безопасность». Оставить её значило бы показать человеку
 *    правило, которого в продукте нет.
 * 5. **Имя руководителя в «нет прав» больше не вписано.** В `OjZtb` стояло
 *    «видит Смирнова Ирина» — образец текста из макета, который каждый агент
 *    читал как имя своего начальника. Имя теперь приходит из состава
 *    агентства, а если состава ещё нет — фразы нет вовсе.
 *
 * Числа сюда не вписаны ни одно: «два часа» и «восемь минут» из кадров —
 * это настройка простоя и возраст данных, оба считаются на месте. Модуль
 * принимает их уже словами, поэтому сам ни от чего не зависит.
 */

/* ── Нет прав (`pZQYx`, `OjZtb`) ───────────────────────────────────────────── */

export const NO_RIGHTS = {
  title: "Раздел только для руководителя",
  /**
   * Названы все четыре раздела, а не «этот раздел».
   *
   * Агент, наткнувшийся на закрытую «Эффективность», через минуту попробует
   * «Сотрудников» — и упрётся снова. Перечисление отвечает сразу на весь
   * список, и второго удара по той же двери не будет.
   *
   * Вторая половина фразы отвечает на невысказанное «а мне включат?»:
   * это не галочка в настройках, а свойство роли. Пока это не сказано,
   * человек будет просить включить ему доступ.
   */
  lead:
    "«Эффективность», «Сотрудники», «Отказы» и «Журнал доступа» видит только руководитель. Это не настройка, а свойство роли: расходы агентства и журнал обращений к контактам не показываются агентам.",
  /**
   * Главное действие тупика.
   *
   * Ролей в системе ровно две, и «дать доступ к разделу» в продукте не
   * существует — есть только передача роли руководителя (окно `Передать роль`).
   * Значит единственный способ что-то изменить — написать тому, кто это
   * решает. Кнопка честнее любого «обратитесь к администратору»: она
   * называет конкретного человека и конкретное действие.
   */
  write: "Написать руководителю",
  leave: "Вернуться в «Сегодня»",
} as const

/**
 * Хвост про руководителя — только когда состав агентства его знает.
 *
 * `OjZtb` называет имя, `pZQYx` не называет. Названное имя полезнее: агент
 * сразу видит, кому писать. Но выдуманное имя вреднее молчания, поэтому
 * фраза появляется ровно тогда, когда имя настоящее.
 *
 * Оборот взят у уже собранного окна передачи роли — «Сейчас ответственный —
 * <имя>», — чтобы в продукте не завелось двух способов назвать одно и то же.
 */
export function noRightsText(ownerName: string): string {
  const clean = ownerName.trim()
  return clean === "" ? NO_RIGHTS.lead : `${NO_RIGHTS.lead} Руководитель агентства — ${clean}.`
}

/* ── Нет связи (`RYNP4`, `r3thk`) ──────────────────────────────────────────── */

export const OFFLINE = {
  /** Строка результата на компьютере: что произошло. */
  header: "Нет связи с сервером",
  title: "Похоже, пропал интернет",
  /**
   * Единственное предложение, набранное в обоих кадрах слово в слово, —
   * и не случайно: это самая дорогая строка экрана.
   *
   * Агент без связи первым делом жмёт «Раскрыть контакт» и, не получив
   * телефона, решает, что деньги списались впустую. Здесь сказано, что
   * списания не было и записи в журнале тоже: обе половины — про то,
   * почему раскрытие вообще требует сервера.
   */
  blocked:
    "Раскрытие контакта заблокировано: пока связи нет, мы не можем ни списать деньги, ни записать событие в журнал доступа.",
  /** Ответ на второй невысказанный вопрос: «а что с уже наработанным?» */
  kept: "Ничего не потеряется: раскрытые контакты и заметки сохранены на сервере",
  retry: "Попробовать снова",
  /**
   * Второй выход. Тупик без него был бы ожиданием: связь может не вернуться
   * ни через минуту, ни через час, а выдача, которую агент уже открыл,
   * читается и без сервера.
   */
  keepWorking: "Работать с тем, что открыто",
} as const

/** «последние данные получены восемь минут назад» — возраст считает экран. */
export function staleNote(ago: string): string {
  return `последние данные получены ${ago} назад`
}

/**
 * Тот же текст, собранный в один абзац для листа снизу.
 *
 * На компьютере он разложен по трём местам панели — строка результата,
 * тело, подвал. На 390 таких мест нет: лист говорит одним абзацем, и порядок
 * фактов тот же, что глаз проходит на большом экране сверху вниз.
 */
export function offlineSheetText(ago: string | undefined): string {
  const head = ago === undefined ? "" : `Последние данные получены ${ago} назад. `
  return `${head}${OFFLINE.blocked} ${OFFLINE.kept}.`
}

/* ── Сеанс истёк (`JsVqc`, `h33cyp`) ───────────────────────────────────────── */

export const SESSION_EXPIRED = {
  title: "Сеанс истёк",
  /**
   * Подсказка стоит только на большом экране — так в кадрах, и это не пропуск.
   * На телефоне ровно то же обещание несёт вторая кнопка: она не рассказывает,
   * где лежит настройка, а открывает её.
   */
  hint: "Время простоя можно изменить в профиле, во вкладке «Безопасность».",
  change: "Изменить время простоя",
  enter: "Войти",
} as const

/**
 * «Вы не работали в кабинете два часа…»
 *
 * Время приходит словами из настройки простоя, а не вписано в текст.
 * `demo-session.ts` требует этого прямо: «эти два числа обязаны быть одним
 * значением, а не двумя совпадающими». Агент, поставивший себе 30 минут,
 * иначе читал бы, что бездельничал два часа.
 */
export function sessionExpiredText(idle: string): string {
  return `Вы не работали в кабинете ${idle}. Войдите заново — раскрытые контакты, история звонков и стоп-лист на месте.`
}
